
//for (inicializacion; condicion; incremento/decremento)
var x = 0;
for (x; x < 5; x++) {
    console.log("El valor de x es:"+x);
  }

  var x = 0;
for (x; x <= 5; x++) {
    console.log("El valor de x es:"+x);//Sentencia 
  }

  var x = 10;
  for (x; x >= 5; x--) {
      console.log("El valor de x es:"+x);//Sentencia 
    }

    // Este ejemplo se ejecuta en infito 
    //var x = 10;
    //for (x; x >= 5; x++) {
    //    console.log("El valor de x es:"+x);//Sentencia 
    //}